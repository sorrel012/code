--연산자, Operator

	1. 산술 연산자
		- +, -, *, /
		- %(없음) > 함수로 제공(mod())

	2. 문자열 연산자
		- +(x) > ||(o)
		※식별자 "컬럼명|바꿀 컬럼명" > 컬럼명 바꾸기 > 별칭(Alias)

	3. 비교 연산자
		- >, <, >=, <=
		- =, <>

	4. 논리 연산자
		- and, or, not
