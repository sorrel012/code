//클래스

-  유효성 검사 후 유효하지 않은 값은 set되지 않을 때 null 에러가 발생한다.
	> private 변수를 선언할 때 기본값으로 초기화 해주면 null 에러가 발생하지 않는다.

- 조건을 만족할 때까지만 출력하고, 조건을 만족하지 않는 부분은 출력하지 않게 하기 위해서는 메소드에서 return문을 적절히 사용한다.
	> return을 사용하면 바로 빠져나갈 수 있기 때문